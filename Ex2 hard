#include<stdio.h>
#include<math.h>
int main()
{
  int a;
  printf("enter a number);
scanf("%d",&a);
double i = sqrt(a);
if(i ==a)
{
prontf("it is a perfect number:);
  }
else
  {
  printf("it is not a perfect number:);
}
return 0;
}
