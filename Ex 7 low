#include cstdio.h>

void swap (int*, int*, int*);

int main()

{

int a,b,c;

Printf(" enter Value of a&b&c");

Scanf ("%d%d%d", &a, &b, &c);

Printf (" before swap");

printf("a=%d、b=%d,c=%d, a,b,c);

Swap (&a, &b, &c);

printf (" after swap:");

printf("a=%d,b=%d; c=%d", a,b,c);

return o;

}

void swap (int* x, int*y, int*z);
{
int temp;
tenp=*x;
*x=*y;
*y=*z;
*z=temp;
}
